# Initial version generated by chatGPT on

import yaml
from jsonschema import validate

def validate_yaml_schema(config):
    # Define the YAML schema
    schema = {
        "type": "object",
        "properties": {
            "url": {"type": "string"},
            "steps": {
                "type": "array",
                "items": {
                    "type": "object",
                    "properties": {
                        "element_locator": {"type": "string"},
                        "element_locator_value": {"type": "string"},
                        "interaction": {"type": "string"},
                        "interaction_value": {"type": "string"}
                    },
                    "required": ["element_locator", "element_locator_value", "interaction"],
                    "additionalProperties": False
                }
            }
        },
        "required": ["url", "steps"],
        "additionalProperties": False
    }

    # Validate the YAML config against the schema
    try:
        validate(instance=config, schema=schema)
        return True
    except Exception:
        return False